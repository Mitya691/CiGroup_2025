<UserControl x:Class="DesktopClient.View.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesktopClient.View"
             xmlns:att="clr-namespace:DesktopClient.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="PlaceholderTextBoxStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border"
             Padding="{TemplateBinding Padding}"
             Background="{TemplateBinding Background}"
             BorderBrush="{TemplateBinding BorderBrush}"
             BorderThickness="{TemplateBinding BorderThickness}"
             CornerRadius="12"
             SnapsToDevicePixels="True">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" />
                                <TextBlock x:Name="textBlock"
                        Margin="2,0,0,0"
                        Foreground="{TemplateBinding BorderBrush}"
                        IsHitTestVisible="False"
                        Text="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"
                        Visibility="Collapsed" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="textBlock" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.56" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF7EB4EA" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF569DE5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <DockPanel LastChildFill="True" >
        <Border 
        MinWidth="300"
        MaxWidth="500" 
        Padding="20"
        CornerRadius="12"
        Background="White"
        HorizontalAlignment="Center"
        VerticalAlignment="Center" Cursor="">

            <StackPanel Cursor="">

                <!-- Форма -->
                <StackPanel.Resources>
                    <!-- Стиль для меток -->
                    <Style x:Key="FormLabel" TargetType="TextBlock">
                        <Setter Property="Margin" Value="0 0 0 5"/>
                    </Style>

                    <!-- Стиль для текстовых полей -->
                    <Style x:Key="FormTextBox" TargetType="TextBox" BasedOn="{StaticResource PlaceholderTextBoxStyle}">
                        <Setter Property="Margin" Value="0 0 0 15"/>
                        <Setter Property="BorderBrush" Value="Gray"/>
                        <Setter Property="BorderThickness" Value="0.5"/>
                        <Setter Property="Padding" Value="8,4,0,4"/>
                    </Style>
                </StackPanel.Resources>

                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Регистрация"
               FontSize="24" FontWeight="SemiBold"
               VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
            Content="✖"
            Command="{Binding CloseCommand}"
            Width="28" Height="28" Padding="0"
            Background="Transparent" BorderThickness="0"
            Foreground="Black"
            HorizontalAlignment="Right" VerticalAlignment="Top"/>
                </Grid>



                <TextBlock Text="Имя" Style="{StaticResource FormLabel}"/>
                <TextBox Tag="Введите имя" Style="{StaticResource FormTextBox}" 
                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Фамилия" Style="{StaticResource FormLabel}"/>
                <TextBox Tag="Введите фамилию" Style="{StaticResource FormTextBox}"
                         Text="{Binding Family, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Отчество" Style="{StaticResource FormLabel}"/>
                <TextBox Tag="Введите отчество" Style="{StaticResource FormTextBox}"
                         Text="{Binding Patronymic, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Почта" Style="{StaticResource FormLabel}"/>
                <TextBox Tag="Введите почту" Style="{StaticResource FormTextBox}"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Должность" Style="{StaticResource FormLabel}"/>
                <TextBox Tag="Введите должность" Style="{StaticResource FormTextBox}"
                         Text="{Binding Post, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Логин" Style="{StaticResource FormLabel}"/>
                <TextBox Tag="Придумайте логин" Style="{StaticResource FormTextBox}"
                         Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Пароль" Style="{StaticResource FormLabel}"/>
                <PasswordBox Tag="Придумайте пароль" Margin="0 0 0 20"
                         BorderBrush="Gray" BorderThickness="0.5"
                         Padding="8,4,0,4"
                         att:PasswordBoxAssist.BoundPassword = "{Binding Password, Mode = TwoWay, UpdateSourceTrigger = PropertyChanged}"/>

                <!-- Кнопка -->
                <Button Content="Зарегистрироваться" 
                    Style="{StaticResource BaseButton}" 
                    Foreground="White" 
                    HorizontalAlignment="Stretch"
                    Command="{Binding RegistrationCommand}" Cursor="Hand"/>
            </StackPanel>
        </Border>
    </DockPanel>


</UserControl>
