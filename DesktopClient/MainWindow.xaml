<Window x:Class="DesktopClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:DesktopClient"
        xmlns:vm="clr-namespace:DesktopClient.VM"
        xmlns:view="clr-namespace:DesktopClient.View"
        mc:Ignorable="d"
        xmlns:att="clr-namespace:DesktopClient.Helpers"
        Title="ООО МакПром — Программа учета зерна" Height="400" Width="500"
        WindowState="Maximized"
        Background="LightGray"
        Style="{StaticResource MaterialDesignWindow}"
        Closing="MainWindow_Closing">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:AutorizationDialogVM}">
            <view:AutorizationView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:RegisterDialogVM}">
            <view:RegisterView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:HomeDialogVM}">
            <view:HomeView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:ReportDialogVM}">
            <view:ReportView/>
        </DataTemplate>
        
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <!-- Фоновая картинка -->
            <ImageBrush ImageSource="/Docs/Images/MainPicture.png"
                        Stretch="UniformToFill"
                        AlignmentX="Center"
                        AlignmentY="Center" />
            <!-- Если картинка лежит как Resource: 
                 ImageSource="pack://application:,,,/Images/background.jpg" -->
        </Grid.Background>

        <!-- Твоя текущая страница рендерится поверх фона -->
        <ContentControl Content="{Binding CurrentViewModel}" 
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch"
                        Margin="0"/>
    </Grid>
</Window>